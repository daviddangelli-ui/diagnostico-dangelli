import streamlit as st import pandas as pd import plotly.express as px import urllib.parse
st.set_page_config(page_title="DANGELLI - Diagnóstico", layout="wide")
st.markdown("""<div style='background-color: #1e2130; padding: 30px; border-radius: 15px; border-left: 10px solid #d4af37; text-align: center;'>
<h1 style='color: #d4af37;'>O que garante a perenidade de uma média empresa?</h1> <p style='color: white; font-size: 18px;'>A reforma tributária de 2026 exige um diagnóstico preciso. Avalie sua prontidão com David D'Angelli.</p> </div>""", unsafe_allow_html=True)
with st.form("diagnostico_completo"):     .st.subheader("1. Identificação")     .nome = st.text_input("Seu Nome Completo")     .empresa = st.text_input("Sua Empresa")     .desafio = st.text_area("Seu maior desafio estratégico hoje")         .st.markdown("### 🏛️ Nível 1: Governança")     .g1 = st.select_slider("Acordo de Sócios formalizado?", options=["1", "2", "3", "4", "5"])     .g2 = st.select_slider("Plano de sucessão estruturado?", options=["1", "2", "3", "4", "5"])            .st.markdown("### 🛡️ Nível 2: Blindagem Técnica")     .b1 = st.select_slider("Cláusulas Tag Along e Shotgun?", options=["1", "2", "3", "4", "5"])     .b2 = st.select_slider("Protocolo Familiar instituído?", options=["1", "2", "3", "4", "5"])         .st.markdown("### ⚖️ Reforma Tributária 2026") ....t1 = st.select_slider("Mapeamento do novo modelo IVA?", options=["1", "2", "3", "4", "5"]) ....t2 = st.select_slider("Simulação para 2026-2033?", options=["1", "2", "3", "4", "5"]) .... ....enviado = st.form_submit_button("GERAR DIAGNÓSTICO")
if enviado: ....if not nome or not empresa:         st.error("Preencha a identificação.")     .else:         st.success("Diagnóstico concluído!")         msg = f"Olá David! Sou {nome} da {empresa}. Concluí meu diagnóstico."         texto_wa = urllib.parse.quote(msg)         .st.link_button("🚀 WHATSAPP DO DAVID", f"{texto_wa}")         .st.link_button("🔗 MEU LINKEDIN", "")
